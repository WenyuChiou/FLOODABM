# Core runtime
numpy>=1.24,<3
pandas>=2.1,<3
PyYAML>=6,<7

# Plotting / Excel / validation (needed for figures, table export, and the
# validation utilities; the core simulation runs without them via --no-plots).
# matplotlib pinned <3.9: 3.9 removed the Axes.boxplot(labels=) API the plotting
# code uses.
matplotlib>=3.8,<3.9
scipy>=1.11,<2
openpyxl>=3.1,<4

# Utils (optional)
joblib>=1.3,<2
tqdm>=4.66,<5

# Raster-to-tract depth-distribution utility
geopandas>=0.14,<2

# Windows fonts (optional)
# fonttools>=4.39,<5
